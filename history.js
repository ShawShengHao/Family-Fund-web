const list=document.querySelector('#history-list'),number=v=>Number(String(v??0).replaceAll(',','')),format=v=>number(v).toLocaleString('en-US');
async function render(){const rows=await fetch('/api/history').then(r=>r.json()).catch(()=>[]);list.innerHTML='';if(!rows.length){list.innerHTML='<p class="history-empty">尚無歷史紀錄。</p>';return}rows.forEach(item=>{const row=document.createElement('article');row.className='history-row';const date=document.createElement('div'),nav=document.createElement('div'),count=document.createElement('div'),actions=document.createElement('div');date.innerHTML=`<strong>${item.updatedAt||String(item.savedAt).slice(0,10)}</strong><small>資料庫編號 #${item.id}</small>`;nav.innerHTML=`<strong>${format(item.nav)}</strong><small>當日基金淨值</small>`;count.innerHTML=`<strong>${(item.holdings||[]).length} 項</strong><small>投資標的</small>`;actions.className='history-actions';const edit=document.createElement('a');edit.href=`admin.html?edit=${item.id}`;edit.textContent='修改';const remove=document.createElement('button');remove.type='button';remove.className='delete';remove.textContent='刪除';remove.onclick=async()=>{if(!confirm(`確定刪除 #${item.id} 的歷史紀錄？`))return;const result=await fetch(`/api/snapshots/${item.id}`,{method:'DELETE'});if(result.ok)render()};actions.append(edit,remove);row.append(date,nav,count,actions);list.append(row)})}render();
